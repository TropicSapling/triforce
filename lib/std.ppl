pointer*2 split(pointer list, splitter) {
	pointer*2 new_list -> [];
	unsigned int i = 0;
	if(arr[>>>] == splitter) {
		new_list += list[__itemID + 1];
	}
	
	return new_list;
}

void delItem(pointer list, unsigned int i) {
    list<size> -= 1;
    
    while(i < list<size>) {
        list[i] = list[i + 1];
		i++;
    }
}
