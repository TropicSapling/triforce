pointer*2 split(pointer list, splitter) {
	pointer*2 arr -> [];
	if(list[>>>] == splitter) {
		arr += list[__itemID + 1];
	}
	
	return arr;
}

void fastReplace(pointer list, toReplace, replacer) {
	if(list[>>>] == toReplace) __item = replacer;
}

void delItem(pointer list, unsigned int i) {
    list<size> -= 1;
    
    while(i < list<size>) {
        list[i] = list[i + 1];
		i++;
    }
}
